version: '3.8'

services:
  pipevideo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pipevideo
    restart: always
    env_file:
      - .env
    networks:
      - caddy_network

networks:
  caddy_network:
    external: true
    name: caddy_network
